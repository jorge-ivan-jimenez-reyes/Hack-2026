# Core/Accessibility

Helpers para que la a11y no sea afterthought.

```
AccessibilityModifiers.swift   ViewModifier reutilizables
HapticFeedback.swift           UINotificationFeedbackGenerator wrap
SpeechAnnouncer.swift          AVSpeechSynthesizer / UIAccessibility.post
```

Checklist de a11y vive en `RUBRIC.md`. Probar VoiceOver SIEMPRE antes de cerrar PR.
