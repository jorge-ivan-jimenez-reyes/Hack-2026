# Core/Camera

AVFoundation: captura de fotos/video, manejo de permisos, preview layer puenteado a SwiftUI.

```
CameraService.swift         AVCaptureSession + permisos
CameraPreview.swift         UIViewRepresentable
```
