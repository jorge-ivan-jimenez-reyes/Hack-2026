# Resources

- `Assets.xcassets/` — imágenes, colores con dark mode
- `MLModels/` — archivos `.mlmodel` o `.mlpackage`
- `Localizable.xcstrings` — strings de UI

Modelos pesados (>50MB) considerar bajarlos al primer launch en vez de bundlearlos.
